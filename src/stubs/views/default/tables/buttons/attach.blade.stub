<div class="btn-group pull-right">
    <button id="{{singularSlug}}-id-{{ ${{singularCamel}}->id }}" class="btn btn-primary pull-right attach-{{singularSlug}}" data-{{singularSlug}}="{{ ${{singularCamel}}->id }}">Dodaj</button>
</div>