<div class="btn-group pull-right">
    <button id="{{singularSlug}}-id-{{ ${{singularCamel}}->id }}" class="btn btn-primary pull-right detach-{{singularSlug}}" data-{{singularSlug}}="{{ ${{singularCamel}}->id }}">Usuń</button>
</div>